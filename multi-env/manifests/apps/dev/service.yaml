apiVersion: v1
kind: Service
metadata:
  name: multi-env-nginx
  labels:
    app.kubernetes.io/name: multi-env-nginx
    app.kubernetes.io/part-of: argocd-applicationset-example
    environment: dev
spec:
  selector:
    app.kubernetes.io/name: multi-env-nginx
  ports:
    - port: 80
      targetPort: 80
