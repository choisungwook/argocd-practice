apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "duplicate_namespace.fullname" . }}-test-connection"
  labels:
    {{- include "duplicate_namespace.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "duplicate_namespace.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
