# For Agents

## 실습환경

- 로컬에서 kind cluster를 사용하여 쿠버네티스를 생성하고 kind cluster config는 @kind-cluster/config.yaml를 따른다.
- Argo CD는 helm chart를 사용한다. helm chart를 사용하어 Argo CD를 설치하는 방법은 @install_argocd_with_helm/README.md를 따른다.

## agent skills

- 문서작성은 akbun style skills를 사용한다. 그리고 존댓말을 사용한다.
